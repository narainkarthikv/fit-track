#example environment to create .env in backend directory of fit-track

# Server Configuration
PORT=5000
NODE_ENV=development

# Database
ATLAS_URI=<MongoDb uri>

# CORS Configuration - Production Ready
# Add your production frontend URLs (comma-separated)
# Example: FRONTEND_URL=https://fit-track.vercel.app,https://www.fit-track.com
FRONTEND_URL=

# Optional: Allow preview deployments (set to 'true' to enable)
# Security Note: Only enable in staging/testing environments
ALLOW_VERCEL_PREVIEW=false
ALLOW_NETLIFY_PREVIEW=false
